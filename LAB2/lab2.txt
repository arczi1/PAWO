1. 
cat /etc/passwd | grep "/bin/bash" 
cat /etc/passwd | grep "bin/bash"
grep bin/bash" /etc/passwd

2.
alias pull="git pull origin master"
--set -x
bash
--kod nei dziala bo jwk otwiera sie nowa powlopke to czesc dziala czesc nie aliasy nie dzialaja mozna np zdefiniowac aliasy na stale kub zdefiniowanie funkcji foo() {echo "RS"}
foo
exit 
export -F foo

3. 
ln -s czarodzieje.txt magicy.txt
ln -s magicy.txt oszusci.txt
ls -lah
rm magicy.txt
cat czarodzieje.txt
oszusci.txt -- powiazanie miekkie zostalo usuniete 

ln czarodzieje.txt magicy.txt
ln magicy.txt oszusci.txt
ls -lah


4.
ps ux | grep  "pts/" > procesy.txt
cat procesy.txt

ps aux | awk '{print $2}' >> procesy.txt
cat procesy.txt

ps aux | tail -n 3 > procesy.txt

 -------------
 cat plik | column  -t | xxd
 
--------------

5.
grep pozawny /etc/passwd
sudo adduser powazny
sudo grep powanzny /etc/shadow
sudo passwd powazny 
sudo login powany

usermod -d /home/domowy powazny 
sudo chown powazny:powany /home/domowy

6. 
ssh-keygen
cat ~/.ssh/id_rsa

 
 

